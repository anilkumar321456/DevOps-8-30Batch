apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: account-api
  template:
    metadata:
      labels:
        app: account-api
    spec:
      containers:
      - name: account-api
        image: nginx
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: account-api-service
spec:
  selector:
    app: account-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
